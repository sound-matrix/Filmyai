/**
 * RETIRED (SOU-13) — mock localStorage admin gate removed from the live path.
 * Use `./admin-auth` (AdminAuthProvider / useAdminAuth) instead.
 * This file is kept only as a breadcrumb so old docs/PRs do not reintroduce the stub.
 */
export {};
